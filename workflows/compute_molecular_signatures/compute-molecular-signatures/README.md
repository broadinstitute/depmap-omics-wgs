# Compute molecular signatures

This module contains utilities for computing molecular signatures (currently just loading the unfiltered variants Parquet file and converting it to a MAF compatible with [SignatureAnalyzer](https://github.com/broadinstitute/SignatureAnalyzer)).
